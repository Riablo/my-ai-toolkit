interface:
  display_name: "My Feishu"
  short_description: "向白名单群发送飞书群消息，并新建或编辑飞书云文档"
  default_prompt: "Use $my-feishu to send Feishu group messages only to approved whitelist chats, mention users by resolving names to open_id when requested, create Feishu docs, edit explicitly provided docs, and troubleshoot login or permission failures when needed."
policy:
  allow_implicit_invocation: false
